const Contact = ()=>{
    return <h1>Contact Us Page</h1>
}

export default Contact

              {/* {(menuItems?.card?.itemCards?.map((item)=><li key={item?.card?.info?.id}>{item?.card?.info?.name}</li>))} */}
